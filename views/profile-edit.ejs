<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Edit Profile</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.1.1/css/bootstrap.css">
</head>
<body>
    <form enctype="multipart/form-data" action="/profile/update" method="post">
    
        <input class="form-control form-control-lg" type="text" name="name" placeholder="Name" value="<%= agent.agent_name %>">
        <input class="form-control form-control-lg" type="email" name="email" placeholder="Email" value="
        <%= agent.agent_email %>">
        <textarea name="bio" placeholder="Bio" cols="30" rows="10"><%= agent.bio %></textarea>
        <div class="form-group">
            <label for="exampleFormControlFile1">Upload Profile Image</label>
            <input type="file" name="upload" accept=".jpg, .jpeg, .png" class="form-control-file" id="exampleFormControlFile1" value="<%= agent.IMG_url %>">
        </div>
        <input class="form-control form-control-lg" type="text" name="location" placeholder="Location" value="<%= agent.location %>">
        <small id="emailHelp" class="form-text text-muted">Please check all applicable</small>
        <div class="form-check">
            <%if (agent.home) {%>
                <input class="form-check-input" type="checkbox" name="home" checked>
            <%} else {%>
                <input class="form-check-input" type="checkbox" name="home">
            <%}%>
            
            <label class="form-check-label" for="home">
                Home
            </label>
        </div>
        <div class="form-check">
            <%if (agent.car) {%>
                <input class="form-check-input" type="checkbox" name="car" checked>
                <%} else {%>
                    <input class="form-check-input" type="checkbox" name="car">
                    <%}%>
                    
                    <label class="form-check-label" for="car">
                        Car
                    </label>
        </div>
        <div class="form-check">
            <%if (agent.life) {%>
                <input class="form-check-input" type="checkbox" name="life" checked>
                <%} else {%>
                    <input class="form-check-input" type="checkbox" name="life">
                    <%}%>
                    
                    <label class="form-check-label" for="life">
                        Life
                    </label>
        </div>
        <input class="form-control form-control-lg" type="password" name="password" placeholder="Password">
        <input class="form-control form-control-lg" type="password" name="confirm" placeholder="Confirm Password">
        <br>
        <button type="submit" class="btn btn-primary">Confirm Changes</button>
    </form>
</body>
</html>