<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title><%= agent.agent_name %> Profile</title>
    <style>
        
    </style>
</head>
<body>
    <h1>Welcome <%= agent.agent_name %></h1>
    <img src="<%= agent.IMG_url %>" height="300" width="300">
    <p><%= agent.bio %></p>
    <ul>
        <%if (agent.home) {%>
            <li>Home</li>
        <%}%>
        <%if (agent.car) {%>
            <li>Car</li>
        <%}%>
        <%if (agent.life) {%>
            <li>Life</li>
        <%}%>
    </ul>
</body>
</html>